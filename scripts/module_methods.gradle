ext {

    applyViewBinding = { module ->
        module.configure(module) {
            android {
                buildFeatures {
                    viewBinding true
                }
            }
        }
    }
}